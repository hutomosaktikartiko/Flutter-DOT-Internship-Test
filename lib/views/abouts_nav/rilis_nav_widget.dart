import 'package:flutter/material.dart';

class RilisNav extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Versi 1.0.0"),),
    );
  }
}