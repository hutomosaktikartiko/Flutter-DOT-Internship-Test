import 'package:flutter/material.dart';

class AboutNav extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("DOT Flutter Test"),),
    );
  }
}